# Secrets — never commit Langfuse / Anthropic keys
.env
.env.local
# team-distributed shared Langfuse eval keys (out-of-band, never committed)
.env.shared

# Local virtualenv
.venv/

# Generated / cached experiment artifacts (path-1 regen outputs)
goldens/**/*.generated.*
.cache/
__pycache__/
